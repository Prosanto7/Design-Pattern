package ChainOfResponsibilityPattern;
/*import java.util.Comparator;

public class NameSorter extends Sorter{
    public NameSorter(Sorter s) {
        this.insideSorter = s;
    }

    public Comparator<Student> Comparator = new Comparator<Student> () {
        public int compare(Student s1, Student s2) {
            if (s1.name.equals(s2.name) && insideSorter != null) {
                //return insideSorter.Comparator(s1,s2);
            }
            return s1.name.compareTo(s2.name);
        }
    };
}*/
